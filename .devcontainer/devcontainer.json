{
  "build": {
    "dockerfile": "Dockerfile.github"
  },
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md"
      ]
    }
  },
  "mounts": [
    "source=${localEnv:HOME}/Documents,target=/mnt/host-documents,type=bind,consistency=cached"
  ]
}